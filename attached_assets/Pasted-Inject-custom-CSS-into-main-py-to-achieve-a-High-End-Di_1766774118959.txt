Inject custom CSS into `main.py` to achieve a "High-End Diplomatic" UI.

1. Import a Google Font (Playfair Display) for headings.
2. Insert this `apply_custom_style()` function at the start of your script and call it inside `main()` immediately after `init_session_state()`.

```python
def apply_custom_style():
    st.markdown("""
        <style>
        /* Importando Fonte de Luxo para Títulos */
        @import url('[https://fonts.googleapis.com/css2?family=Playfair+Display:ital,wght@0,400;0,700;1,400&display=swap](https://fonts.googleapis.com/css2?family=Playfair+Display:ital,wght@0,400;0,700;1,400&display=swap)');

        /* Títulos Principais (H1) */
        h1 {
            font-family: 'Playfair Display', serif;
            color: #2C3E50;
            font-size: 3rem !important;
            font-weight: 700;
            text-align: center;
            border-bottom: 2px solid #E6D2D5; /* Linha sutil em Rose Gold */
            padding-bottom: 20px;
            margin-bottom: 30px;
        }
        
        /* Subtítulos (H2, H3) */
        h2, h3 {
            font-family: 'Playfair Display', serif;
            color: #4A4A4A;
            font-weight: 400;
        }

        /* Cartões de Métricas e Informações */
        div[data-testid="stMetric"], div.stInfo, div.stSuccess, div.stWarning, div.stError {
            background-color: #FFFFFF;
            border: 1px solid #F0E6E8;
            border-radius: 15px; /* Bordas arredondadas femininas */
            box-shadow: 0 4px 6px rgba(0,0,0,0.02); /* Sombra muito suave */
            padding: 15px;
        }

        /* Botões */
        div.stButton > button {
            background-color: #FFFFFF;
            color: #B86E7E;
            border: 1px solid #B86E7E;
            border-radius: 25px; /* Botões pílula */
            font-family: 'Source Sans Pro', sans-serif;
            font-weight: 600;
            transition: all 0.3s ease;
        }
        
        div.stButton > button:hover {
            background-color: #B86E7E;
            color: #FFFFFF;
            border-color: #B86E7E;
            box-shadow: 0 4px 10px rgba(184, 110, 126, 0.3);
            transform: translateY(-2px);
        }
        
        /* Botões Primários (Ação Principal) */
        div.stButton > button[kind="primary"] {
            background-color: #B86E7E;
            color: white;
            box-shadow: 0 4px 10px rgba(184, 110, 126, 0.2);
        }

        /* Área de Texto (Parecer Papel Timbrado) */
        .stTextArea textarea {
            background-color: #FFFFFF;
            border: 1px solid #E0E0E0;
            border-radius: 10px;
            font-family: 'Georgia', serif; /* Fonte de escrita confortável */
            font-size: 16px;
        }

        /* Barra de Progresso em Rose */
        .stProgress > div > div > div > div {
            background-color: #B86E7E;
        }
        
        /* Ocultar elementos padrão do Streamlit */
        #MainMenu {visibility: hidden;}
        footer {visibility: hidden;}
        header {visibility: hidden;}
        </style>
    """, unsafe_allow_html=True)